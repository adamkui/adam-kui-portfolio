export default function PortfolioItem({title, description, tags}) {
    return (
        <div className='portfolio__project'>
            <h1 className='portfolio__projectTitle'>{title}</h1>
        </div>
    )
}
